package fuzz.Robot;

/**
 * A simple record representing a point on a 2D grid, with row and column coordinates.
 * <p>
 * This is used to track positions on the game board.
 *
 * @param row The row index of the point on the grid.
 * @param col The column index of the point on the grid.
 */
record Point(int row, int col){
}